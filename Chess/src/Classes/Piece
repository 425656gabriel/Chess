public abstract class Piece {
    protected boolean isWhite;
    protected int x, y; // Adding these allows the Piece to know its location

    public Piece(boolean isWhite, int x, int y) {
        this.isWhite = isWhite;
        this.x = x;
        this.y = y;
    }

    public boolean isWhite() { return isWhite; }
    
    public boolean isBlack() { return !isWhite; }

    // Every piece will use this to define its unique movement math
    public abstract boolean isValidMove(Board board, int endX, int endY);
}
